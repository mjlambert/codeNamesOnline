'use strict';

//==============================================================================
//  Modules
//==============================================================================

var gameAPI = require('codeNamesOnline/api/gameAPI');

//==============================================================================
//  Exports
//==============================================================================

module.exports = function (io) {

	io.on('connection', function (socket) {

		socket.on('addGameSocket', function (gameCode) {
			gameAPI.AddSocket(gameCode, socket, function (error) {
				if (error) {
					// TODO Make generic error event to send errors to the frontend
					console.log(error);
				}
			});
		});

		socket.on('addPlayerSocket', function (gameCode, team) {
		});

	});

};
