'use strict';

module.exports = function (name, team, socketId) {
	var name     = name;
	var team     = team;
	var socketId = socketId;

	if (!name || name === '') {
		name = 'JIM';
	}
	if (name === 'J' || name === 'j') {
		name = 'ARSEHAT';
	}
	// Getters

	this.getName = function () {
		return name;
	};

	this.getTeam = function () {
		return team;
	};

	this.getSocket = function () {
		return socket;
	};
};
