'use strict';

moudle.exports = function (team, socket) {
	var team   = team;
	var socket = socket;

	this.getTeam = function () {
		return team;
	};

	this.getSocket = function () {
		return socket;
	};
};
